name: Publisher Contacts - Legacy (retired)

# Kept as a manual notice, not a second recurring crawler.
# Active production workflow: admaven-operator-b1669609.yml
on:
  workflow_dispatch:

permissions: {}

jobs:
  active-workflow:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Identify the active workflow
        run: |
          echo 'The active scheduled scanner is AdMaven Contacts - Operator.'
          echo 'Workflow file: .github/workflows/admaven-operator-b1669609.yml'
          echo 'Encrypted results: operator-results-b1669609/index.json'
          echo 'Open the private Contact Desk delivered to the operator.'
